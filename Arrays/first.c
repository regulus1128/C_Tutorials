#include <stdio.h>
//array is a collection of homogenous data items
//general syntax of an array:  (data type of array) (name of array)[size of array]
//size of array can never be empty or a negative number
//all the elements in the array should be of the same data type
//index of array starts with zero and ends with (size of array - 1)

int main()
{
    int b;
    int a[b];
    int arr[0];
}