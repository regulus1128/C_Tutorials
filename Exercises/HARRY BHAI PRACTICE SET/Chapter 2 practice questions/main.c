#include <stdio.h>

int main(){
    int a;
    printf("Enter a number: \n");
    scanf("%d", &a);
    if (a % 3 == 0);
    {
        printf("%d is divisible by 3", a);
    }
    else
    {
        printf("%d is not divisible by 3", a);
    }

    
    
    
}